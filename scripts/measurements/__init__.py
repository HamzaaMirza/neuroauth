"""Measurements that back committed design decisions. Evidence, not results.

Nothing here is judged by a threshold. Each script names the decision it supports and
writes summary statistics only, never a feature vector (hard rule 3).
"""
